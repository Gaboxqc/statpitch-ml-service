# StatPitch v2 — runtime + training dependencies
# NFR-1: everything here is free and open source.

# --- data ---
pandas>=2.2
numpy>=1.26
pyarrow>=16.0          # parquet for the match/odds store
requests>=2.32
beautifulsoup4>=4.12
lxml>=5.2
cloudscraper>=1.2.71   # Transfermarkt (Phase 2)

# --- modelling ---
scipy>=1.13            # brentq (devig), SLSQP (simultaneous Kelly)
scikit-learn>=1.5
xgboost>=2.1
shap>=0.46             # FR-32 per-bet explainability
optuna>=3.6

# --- serving ---
fastapi>=0.115
uvicorn[standard]>=0.30
pydantic>=2.8

# --- dev ---
pytest>=8.3
pytest-cov>=5.0
ruff>=0.6
# Required by fastapi.testclient, which nothing imports by name — starlette
# reaches for an HTTP client at import time and raises without one. That made it
# invisible to a dependency audit and invisible locally, where it was present as
# somebody else's transitive dependency; the first clean install (CI) failed
# collection outright. httpx2 rather than httpx: starlette deprecated the latter.
httpx2>=2.9
